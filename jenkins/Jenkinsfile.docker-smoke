pipeline {
    agent any

    stages {
        stage('Verify Docker from Jenkins') {
            steps {
                sh 'id'
                sh 'docker --version'
                sh 'docker ps'
            }
        }
    }
}
