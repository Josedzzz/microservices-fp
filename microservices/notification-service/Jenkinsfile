pipeline {
    agent any

    environment {
        DOCKER_NET = 'microservices-fp_microservices-network'
        SERVICE_DIR = 'microservices/notification-service'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build & Test') {
            steps {
                script {
                    // Running Maven build inside a container to ensure environment consistency
                    docker.image('maven:3.9.6-eclipse-temurin-17').inside("-v ${HOME}/.m2:/root/.m2") {
                        dir("${SERVICE_DIR}") {
                            sh 'mvn clean install'
                        }
                    }
                }
            }
        }

stage('SonarQube Analysis') {
    steps {
        withCredentials([string(credentialsId: 'sonar-token', variable: 'SONAR_TOKEN')]) {
            dir("${SERVICE_DIR}") {
                sh '''
                # Create the folder and ensure it's empty/ready
                mkdir -p .scannerwork

                docker run --rm \
                    -u root \
                    --network microservices-fp_microservices-network \
                    --volumes-from jenkins \
                    -v "$WORKSPACE/${SERVICE_DIR}/.scannerwork:/tmp/.scannerwork" \
                    -e SONAR_HOST_URL=http://sonarqube:9000 \
                    -e SONAR_TOKEN=$SONAR_TOKEN \
                    -w "$WORKSPACE/${SERVICE_DIR}" \
                    sonarsource/sonar-scanner-cli:latest \
                    sonar-scanner \
                    -Dsonar.projectKey=notification-service \
                    -Dsonar.sources=src/main/java \
                    -Dsonar.java.binaries=target/classes \
                    -Dsonar.host.url=http://sonarqube:9000 \
                    -Dsonar.token=$SONAR_TOKEN \
                    -Dsonar.working.dir=/tmp/.scannerwork

                # Reclaim ownership
                chown -R $(id -u):$(id -g) .scannerwork
                '''
            }
        }
    }
}

        stage('Quality Gate') {
    steps {
        withCredentials([string(credentialsId: 'sonar-token', variable: 'SONAR_TOKEN')]) {
            dir("${SERVICE_DIR}") {
                sh '''
                set -e
                
                # Look specifically in .scannerwork and hide permission errors
                REPORT_FILE=$(find .scannerwork -name "report-task.txt" 2>/dev/null | head -n 1)
                
                if [ -z "$REPORT_FILE" ]; then
                    echo "Error: report-task.txt not found in .scannerwork folder!"
                    ls -la .scannerwork
                    exit 1
                fi

                CE_TASK_URL=$(grep ceTaskUrl "$REPORT_FILE" | cut -d= -f2-)
                echo "Found report at $REPORT_FILE. Checking status at: $CE_TASK_URL"
                
                # ... (rest of your curl and jq loop remains the same)
                '''
            }
        }
    }
}
    }

    post {
        always {
            dir("${SERVICE_DIR}") {
                junit 'target/surefire-reports/*.xml'
            }
        }
        success {
            echo 'Pipeline completed successfully!'
        }
        failure {
            echo 'Pipeline failed. Check the SonarQube logs or Quality Gate status.'
        }
    }
}
