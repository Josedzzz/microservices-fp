package com.microservices.notification.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // Local security filters removed. Validation is handled by the API Gateway.
}
